<div class="col-md-3">
  <%= link_to url = rails_blob_path(photo.image) do %>
    <div class="image">
      <%= image_tag rails_blob_path(photo.image) %>
    </div>
  <% end %>
  <div class="title">
    <p><%= photo.title %></p>
    <p><%= photo.created_at.to_s %></p>
  </div>
</div>

